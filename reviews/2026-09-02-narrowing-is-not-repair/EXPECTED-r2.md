# Pre-registered expectations, round 2 gate (written before either verdict was read)

1. Obligation (2) in the op-rigor rule ("name every forced condition… or the rule
   asserts the other condition alone") — the tail clause is compressed to the point
   of ambiguity. Expect a Medium asking for plain wording. Likely real.
2. "a noun that is not measured is measured or the line is weakened" — awkward,
   possibly read as circular. Expect a Low/Medium density or clarity finding.
3. §3 "the ledger now marks closed a finding that is open" — predictive, not a
   clearance condition; round 1 raised a sibling (luna F12 was on the other rule).
   Expect a Low.
4. ✅ example "with the forced validation mode named" — the 08-31 README says the
   probe "forces condition 2 with UNCHECKED_HASH"; whether the verdict LINE names
   it is not in the README. Expect NOT CONFIRMED on the ✅'s fidelity. If raised,
   the honest fix is to weaken the ✅ to what the README states.
5. Adjudication row 3's "grok's derivation matches mine" leans on reviewer
   agreement as corroboration — a sol reviewer may (correctly) note that
   agreement is not corroboration per the pack's own §3. Expect Low; fix by
   restating the reason on merit.
6. Provenance: "marked the fixture's forced validation mode an unreported
   co-cause" — supported by README round-2 row 2. Should pass.

Not expected: portability (grep clean), heading/body conflict (rewritten).
