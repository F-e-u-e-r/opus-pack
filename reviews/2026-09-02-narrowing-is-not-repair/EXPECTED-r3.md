# Pre-registered expectations, round 3 (written before either verdict was read)

1. "observations sufficient for it" — a reviewer may ask what "sufficient" means
   operationally (it is doing the work "measured" did before). Expect Medium:
   "sufficient" undefined. Possible fix: the parenthetical list already gives the
   standard; could add "sufficient = the line could not be printed with the
   relation false". Real if raised.
2. Obligation (2) "or the claim asserts the remaining conditions alone" — still a
   compressed consequence clause. Expect Low.
3. §3 heading now says "no wider than what was measured" but the body allows
   substantiation without a probe for non-empirical claims — "measured" vs
   "substantiated" mismatch. Expect Medium. Fix: "than what was established".
4. ✅ "naming the forced validation mode in the line" — README does not say the
   verdict line names it (it says the probe "reports them separately"). Expect
   NOT CONFIRMED / fidelity Low. Honest fix: "reporting the forced validation
   mode separately".
5. Provenance "marked… as lacking checked-in evidence" — README row 4 matches.
   Should pass.
Expect PROCEED-with-Lows from at least one reviewer; if both FIX with a new
Medium+ mechanism defect, the cap is reached and I stop and report rather than
run a round 4.
