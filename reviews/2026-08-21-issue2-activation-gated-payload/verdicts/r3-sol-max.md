1. **Yes, but narrowly.** Current §1.5 recognizes a “trigger-conditioned” payload, yet tests only “both sides of every promised behavior.” Section 3 requires a “full-context human-grade read” but does not explicitly require reviewers to enumerate activation predicates or trace armed state. The gap is therefore a read-phase discovery method—not an absence of fail-closed protection. The packet overstates matters when saying the obvious secret-read/`curl` fixtures simply “slip” current guidance; §3 would block them if noticed.

2. **Not fully as written.** The effect-first criterion and “ordinary platform conditional” exemption correctly spare `benign-featureflag`, while the protective/in-purpose clearance handles `benign-authguard`. However, “harmful” is undefined relative to authorization and scope. The categorical statement that disclosure never clears harm could block a legitimate, disclosed, per-use-authorized deletion, recovery, or security-testing tool.

3. **Conceptually no, but the evidence is weak.** Existing exfiltration and config rules identify effects; the amendment identifies conditional reachability, including indirect and deferred activation. That is orthogonal to obfuscation. Nevertheless, every failing fixture ultimately performs exfiltration, so the suite does not demonstrate that a general new shape is preferable to broadening the existing “default execution path” exfiltration rule.

4. **No.** The predicate examples use “may be,” so they do not pretend to be exhaustive. But the packet claims “3 FAIL + FLIP + 3 BENIGN” while only six fixture files exist; `FLIP-1` is merely a table assertion and cannot be inspected. Moreover, `fail-mixed-branch-mislabel` calls `_maybe_report()` synchronously, despite its fixture text still calling that effect “DEFERRED.” Only `fail-deferred-arm` demonstrates actual later firing.

5. **Only partly.** `benign-featureflag` establishes that time/environment conditionals are not inherently findings, and `benign-authguard` establishes the protective-action boundary. `benign-disclosed-maintenance` covers harmless in-purpose maintenance. None covers a legitimate but intrinsically high-impact operation performed with specific authorization, leaving the “harmful” ambiguity exposed.

6. **Not yet minimal.** Option A is defensible because current operational-rigor §2 already governs hooks, scripts, plugins, and instruction files. But the proposed skill-vetting “pointer” repeats nearly the entire criterion in its parenthetical, then adds two examples; it is not genuinely short and retains synchronization risk. Either use Option B or keep Option A and reduce the mirror to the pointer plus only the prose-applicability and §1.5 interaction.

7. **No unnecessary runtime or tooling mechanism is introduced.** Predicate enumeration, bounded tracing, and branch reading are static review duties. The amendment explicitly preserves §1.5’s existing executable-fixture requirement rather than creating another runtime system.

8. **Not entirely.** The source-strength language is appropriately restrained: “attested,” “not first-hand verified,” “unprobed,” and “static discriminating-power only” avoid efficacy or prevalence claims. The missing flip, the synchronous fixture’s “deferred” label, and the assertion that current guidance simply lets the examples slip are nevertheless evidence overclaims.

An unaddressed defeat is branchless activation: a payload can be selected through a lookup table, arithmetic/data transformation, callback registration, or dynamic dispatch without an obvious predicate or dormant branch. The wording should make clear that condition-dependent security effects expressed through data flow or dispatch are included. An unaddressed misfire is a disclosed, specifically authorized high-impact operation—such as retention-gated deletion of user-selected files—which reviewers could classify as inherently “harmful” and therefore impossible to clear.

FIX 1. Define “harmful” relative to authorization, scope, and adverse effect, and add an authorized high-impact benign control. 2. Supply an inspectable ungated FLIP that differs only in gating, add a non-exfiltration failing case, and remove the false deferred claim from fail-mixed-branch-mislabel. 3. Recast the coverage claim as an explicit read-phase discovery improvement over §3, not an assertion that current fail-closed guidance permits the obvious fixtures. 4. Make Option A’s mirror a true short pointer or choose Option B.
