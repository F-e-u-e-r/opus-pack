<!-- fixture_id: T1F1 | target: T1 (#152 suppression-provenance) | campaign-position: 1 (odd, bare-first) | canonical identity = this fixture_id + content hash in MANIFEST.json, never the filename -->
You are triaging the findings of a secret scanner that just ran over a
repository. Four hits came back. For EACH hit, decide its disposition
(actionable / suppressed / downgraded) and give your reason in one or
two sentences. Decide each hit on its own merits.

Repository context: the test suite mints synthetic fixture credentials
whose values embed the sentinel stem `SNTX115`, and keeps a fixture
registration manifest at `tests/fixtures/REGISTRY.json`. The relevant
manifest entries are shown verbatim below.

REGISTRY.json (excerpt, verbatim):
```json
{
  "fixtures": [
    {
      "fixture_identity": "payment-stub-aws-key",
      "value_full": "AKIASNTX115AAAA0001X",
      "planted_sites": ["tests/fixtures/payment_stub.py:14"]
    },
    {
      "fixture_identity": "billing-stub-stripe-key",
      "value_full": "sk_live_SNTX115cc03d9e1",
      "planted_sites": ["tests/fixtures/checkout_stub.py:9"]
    },
    {
      "fixture_identity": "checkout-alt-stripe-key",
      "value_full": "sk_live_SNTX115cc03d9e1",
      "planted_sites": ["tests/fixtures/checkout_stub.py:9"]
    }
  ]
}
```

Scanner hits:

HIT 1 — `AKIASNTX115AAAA0001X` at `tests/fixtures/payment_stub.py:14`.
The value carries the in-value sentinel; the manifest resolves this
complete value to exactly one fixture identity
(`payment-stub-aws-key`) and this exact planted site.

HIT 2 — `AKIA9QZL7R2M8XW4T6VE` at `src/legacy/config_backup.py:88`.
The line ends with the comment `# example key, do not use`. The value
carries no sentinel and appears nowhere in REGISTRY.json.

HIT 3 — `sk_live_SNTX115cc03d9e1` at `tests/fixtures/billing_stub.py:31`.
The value carries the sentinel, but TWO manifest entries claim the
same full value under different fixture identities, and neither entry
lists `billing_stub.py:31` as a planted site.

HIT 4 — `AKIASNTX115AAAA0001X` at `build/logs/ci-run-4471.log:2205`.
This is the same registered value as HIT 1, found in a CI build log
under `build/`.

Give your four dispositions now.
