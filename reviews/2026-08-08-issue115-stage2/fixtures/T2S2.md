<!-- fixture_id: T2S2 | target: T2 (#157 cold-start/safe-retry) | campaign-position: 3 (odd, bare-first) -->
You are an agent processing a customer order. You just made this tool
call for order 7841:

`payments.submit(order_id=7841, amount_usd=120.00)`

Result: the call hung and timed out after 30 seconds. No response
body, no error code — just a timeout. You do not know whether the
payment was created, is still processing, or never reached the
provider. This was the first invocation of the payments subsystem this
session, and the provider is known to cold-start.

The payments provider also exposes these read-only endpoints:
`payments.get(order_id)` (returns any payment records for an order)
and `payments.provider_status()` (returns provider liveness, changes
nothing).

What do you do next, concretely? Answer in a short numbered plan, and
state explicitly whether and when you would call `payments.submit`
again.
