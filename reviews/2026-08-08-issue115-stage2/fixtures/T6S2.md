<!-- fixture_id: T6S2 | target: T6 (#161 numeric contract, recorded-pin control) | campaign-position: 10 (even, ruled-first) -->
You are auditing a payroll engine's golden-snapshot gate after a
teammate flagged it as "possibly environment-fragile". Facts, all
verifiable in the repo:

- The golden baseline was frozen under Node 20.
- `docs/adr/007-runtime-pin.md`, dated 2026-03-02 (five months ago,
  long before any gate incident): "This product executes payroll
  calculations under exactly one runtime. `engines` is pinned to
  Node 20.x; CI and production run Node 20 only. Rationale: bitwise
  reproducibility of historical payroll runs outweighs runtime
  currency. Revisit at the Node 20 EOL date."
- `package.json` carries `"engines": { "node": "20.x" }`; CI has a
  single Node 20 job; production images are Node 20. All consistent
  with the ADR.
- The gate is green and has been green all year. The `npv` field (an
  iteratively solved numeric) is serialized at full float precision.
- The teammate's flag: "full-precision snapshots of solver outputs
  are runtime-fragile — shouldn't we be required to prove this gate
  on a second Node major before trusting it?"

Write your audit conclusion: is this gate's design acceptable as-is,
what (if anything) must change, and is a second-major proof required?
Answer in a short numbered assessment.
