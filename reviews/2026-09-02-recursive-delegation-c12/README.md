# ⑫ recursive-delegation design gate — evidence package

Design-only landing: the ⑫ delegated-authority / contribution-provenance
propagation rules (verdict B, abstraction L2) landed into
delegation-and-review §2 (canonical re-delegation bullet, carrying the
single `unprobed` marker) and §3 (first-person/accounting bullet), plus
cross-model-review §1's transitive reviewer-family sentence (family
propagation only; delegation-and-review stays the sole authority). ⑧-A
(the execution-principal bullet and its capability-receipt reference) is
byte-untouched. No runtime/tooling change; no max-depth rule; the
orphan-principal discovery is expressly NOT activated.

Authoritative design source: `design-v2.md` (frozen bytes that passed the
R2 gate 2/2). The landed doctrine text is a faithful transplantation of
its three operative blocks — Markdown placement adaptations only —
machine-proven by `c12_checks.py` (run it from the repository root).

## Contents

- `orientation-report.md` — the eleven orientation deliverables (durable).
- `probe-evidence.md` — the inert two-level spawn probe, expectations
  written before results.
- `design-v1.md` / `design-v2.md` / `v1-to-v2.diff` — the design lineage;
  the diff's per-hunk F1/F2 attribution is embedded in `packet-r2.md`.
- `packet-r1.md` / `verdict-luna-r1.md` / `verdict-sol-r1.md` — round 1
  (1/2: luna PROCEED, sol FIX×2 — NOT passed).
- `adjudication-f1-f2.md` — both findings reproduced + validated; the
  exactly-two-corrections authorization.
- `packet-r2.md` / `verdict-luna-r2.md` / `verdict-sol-r2.md` — the
  narrow confirmation round (2/2 PROCEED → DESIGN PASSED @ frozen v2).
- `identity-and-ops.md` — identity banners for all four runs, gate log,
  and the quota-block record (operational provenance only, never
  semantic evidence).
- `c12_checks.py` — the faithful-reconstruction battery (see its
  docstring for the check list).
- `MANIFEST.sha256` — script-generated hashes of every file above.

Review lineup honesty: both rounds ran two variants of one provider
family at max effort — a dual-blind two-variant gate, NOT a cross-family
gate (recorded in delegation-and-review's Provenance entry as well).
