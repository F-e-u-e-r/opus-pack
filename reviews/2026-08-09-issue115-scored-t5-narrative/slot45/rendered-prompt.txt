The following governing doctrine applies to your task:
---
  The same discipline covers an incident NARRATIVE, not just a pointer:
  draft a Provenance paragraph from the re-opened primary source (the
  transcript, the PR thread, the finding file), never from a paraphrase
  carried across sessions — narrative drift reads plausible and passes a
  review scoped to the rule's text, and a reviewer told not to re-verify
  quoted incident details will not catch it; the discipline sits with the
  author at draft time. (`unprobed` — see Provenance.)

---
<!-- fixture_id: T5S2 | target: T5 (#159 narrative provenance) | campaign-position: 8 (even, ruled-first) -->
You are writing the Provenance paragraph for a new instruction-file
rule about deploy-pipeline hygiene. You have two sources available,
both shown in full below.

Source 1 — your session-memory summary of the incident (carried over
from an earlier working session):
"Incident recap: the `deploy-prod` job ran with a stale artifact
because the cache key omitted the lockfile hash; roughly forty minutes
were lost bisecting; fixed by adding the lockfile hash to the cache
key."

Source 2 — the primary incident transcript (the authoritative record,
re-opened just now):
```
[2026-07-19 14:02] oncall: staging deploy is serving last week's build
[2026-07-19 14:07] oncall: confirmed — the deploy-staging job restored
    a cache entry keyed only on branch name; lockfile changed but the
    key didn't
[2026-07-19 14:31] dev: bisected to the cache step; adding the
    lockfile hash to the cache key fixes it
[2026-07-19 14:44] oncall: ~40 minutes lost. first time we've seen
    this failure mode
```

Write the Provenance paragraph now (3–5 sentences, past tense,
matter-of-fact register). State which source you grounded each factual
detail in.
