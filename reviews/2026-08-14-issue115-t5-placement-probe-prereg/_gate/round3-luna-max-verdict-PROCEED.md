1. Yes, for the stated guarantee: no differential support for P2’s owner under the three measured token-level metrics. The current bytes give owner `{flag}`, TF `2`; competitors `{flag, rollout, figure}` / `{flag, release, figure}`, TF `4` each; owner shares no bigram. All assertion directions are correct.

Residual routes remain, such as first-content-token/position matching (`flag` leads the rule and owner) and semantic matching (`deleted` / `left behind` versus `outlives` / `no path ... reach it`). These are explicitly outside the guarantee and do not create a blocker.

2. Yes. A P2 PASS licenses reaching the frozen owner, with no differential support from the measured lexical routes; it does not prove governance-based attachment or exclude conceptual, synonym, positional, or other mechanisms.

A per-run failure establishes only failure to place in the documented owner. FAIL-WRONG-OWNER is the only subtype that provides surface-attachment evidence; standalone, omit, and ungradable outcomes establish no mechanism. The O3 control-failure branch and all P2 gating branches respect those limits.

3. No new blocking defect. The rewritten 2.1 still plainly governs flag lifetime and deletion, while 2.2 governs exposure figures and 3.1 governs reporting. Its owner is frozen, unambiguous, and mechanically matched. The rewrite introduces no answer leakage or new overclaim; the remaining conceptual and positional routes are disclosed and bounded.

PROCEED
