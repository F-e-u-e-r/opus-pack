1. For the stated four-axis guarantee, yes. The current bytes give owner overlap `{flag}`, TF score 2, no shared bigram, and character-4-gram overlap `{flag}` with no owner-exclusive 4-gram. Competitors score 4 and share additional types, so the assertion directions are correct; the prior `live` route is closed.

A broader subword route remains: under character 3-grams, `its` and `one` are owner-exclusive (`its` appears in both rule and owner; `one` occurs in owner’s `none`), absent from the other bullets. This is non-blocking because the prereg explicitly limits the claim to the four measured axes and disclaims exhaustive surface isolation.

2. Mostly, but not exactly. The rubric, §4, §12, and §8’s main PASS/FAIL boundaries are correctly scoped. However, §8’s O3 branch still says the owner offers “no token-level route,” and R3 says “not token-level surface attachment.” The owner does share the universal token `flag`, and the control measures four axes, not merely token-level overlap. Those phrases must be aligned with “no differential route on the measured surface axes.”

3. No other repair-related blocker exists. The residual 3-gram route does not make the design unidentifiable under its explicitly bounded claim, and no additional fixtures, arms, or runs are required.

HOLD: 1. Replace the stale “no token-level route” and “not token-level surface attachment” language in the §8 O3 and R3 branches with the exact four-axis measured-surface boundary.
