The substantive leakage repair works, but the current package still has three factual inconsistencies.

1. **§8 terminology — partial confirmation.** The O3/R5 and R3 branches no longer use `token-level`; “no differential route on any measured surface axis” and “not reachable by the measured surface routes” match the intended boundary. However, §8 later calls it a **“four-measure lexical invariant”**, while §4 enumerates five axes, (i)–(v).

2. **Blocker B — mechanically gone from P2, but stale quotations remain.** In the actual fixture:

   - Raw rule/owner overlap is `{flag, the}`, both available elsewhere.
   - At n=3, owner overlap is `{fla, lag, out, ted, the}`, all available elsewhere.
   - At n=4, it is `{flag}`, available elsewhere.
   - At n=5…8, it is empty.

   Thus `its` and `one` are no longer owner-exclusive routes; the raw axis does not hide function words. But prereg §4 and R-P2 still quote the old sentence, “Record the clearing release beside **its** figure,” instead of the current “beside **the** figure.”

3. **Invariant coverage — confirmed.** `raw_tokens()` is unfiltered and unstemmed; `range(3, 9)` covers every width 3–8; and `owner_exclusive()` exactly computes `(rule ∩ owner) − ⋃(rule ∩ other)`.

4. **Axis labelling — confirmed.** The content-token axis is identified as stopword-filtered and plural-stemmed, while raw tokens are separately identified as unfiltered and unstemmed.

5. **History — mostly preserved, but internally inconsistent.** R-P2 and the prereg truthfully describe four failed drafts. The §8b script comment still says “Three successive drafts” while enumerating all four.

6. **Frozen semantics — confirmed from the provided checks and bytes.** P1 byte equality, the current-clause anchor, rendered P1×B/P1×C hashes, arms, predicate, schedule, budget, and isolation walls remain guarded and unchanged.

7. **Check support — qualified confirmation.** The normal manifest-present path contains 164 checks, and the executable checks support the core raw-token and n=3…8 owner-exclusivity claims. They do not detect the stale quotation, stale measure count, or incorrect draft count, which is why all 164 can pass despite those defects.

8. **Identifiability — preserved.** Removing accidental owner-exclusive cues strengthens P2’s intended capability-control role. The acknowledged conceptual, positional, and unmeasured-mechanism limitations remain bounded. No additional fixture, arm, or run is required.

9. **New blockers.** There is no new executable leakage or identifiability blocker, but the three current-byte documentation inconsistencies above prevent final confirmation.

HOLD: 1. Prereg §4 and R-P2 quote the obsolete “beside its figure” wording instead of the current “beside the figure.” 2. Prereg §8 incorrectly calls the five-axis invariant “four-measure.” 3. static_checks.py says “Three successive drafts” while enumerating and elsewhere documenting four.
