# Final-delta reconstruction — landed bytes vs #225 current source

**Claim:** landed canonical delta = **#225 current semantics (frozen diff sha256 `3c27312b…`, head `d04e814`) + exactly two declared corrections (MOD-C2c, owner-authored; MOD-DATE, gate-finding-driven) + nothing else.**

**MOD-DATE (r1 gate finding, both reviewers converged):** #225's rule-text marker dated the addition-side over-report to the 2026-08-28 fixtures while its own provenance placed results (11)–(12) under 2026-08-29. Landed text splits the dates by result everywhere (rule-text marker and provenance opening), bound to git 2.50.1.

**Machine proof at landing:** the line-multiset difference between the landed diff (vs main `85d3629`) and #225's frozen diff is exactly: 5 authored lines replacing 1 source line — the MOD-C2c region ("after `prune` removed the administrative entry, the bare repair invocation returned success while leaving the pointer unrecovered and the path form errored — the recovery path is gone even though the command need not fail loudly, so exit status alone cannot prove a repair succeeded; verify the recovered pointer/state"), replacing #225's "after `prune` removed the entry, both failed and the pointer stayed gone" — the sole first-hand divergence found in the reproduction gate (the bare form is an exit-0 silent no-op, not a process-level failure). Zero extra deletions, zero other insertions. **Zero undeclared semantic drift.** #220's earlier intermediate wordings are lineage only — not a second source, and their falsified intermediates remain visible in the contributor PR history (nothing laundered).
