1. Axis 1 — Pass. C1–C14 are faithfully represented.

2. Axis 2 — Pass. No old prune premise, merge-preview conflation, addition-side authority, or dead-cwd mislabel remains.

3. Axis 3 — Pass. Materialization, merge preview, content/commit reads, and delete-risk reads use distinct commands.

4. Axis 4 — Pass. `log` and three-dot `diff` are explicitly complementary, including commit-plus-revert divergence.

5. Axis 5 — Pass. C2c correctly distinguishes the exit-0 silent no-op from the path-form exit-1 behavior.

6. Axis 6 — Finding 1, blocking: the marker in `@@ -281,21 +281,68 @@` says the addition-side over-report was verified on `2026-08-28`, while `@@ -1424,11 +1474,64 @@` records it under `Probed 2026-08-29`. The opening provenance sentence also broadly assigns all mechanics to August 28. Split or correct these dates; the version binding to Git 2.50.1 is otherwise present.

7. Axis 7 — Pass. Squash, empty-two-dot, and both incident shapes remain `unprobed`; no guidance-effectiveness claim is made.

8. Axis 8 — Pass. The provenance records the refuted original reading, old prune text, contributor incident, and owner correction without laundering history.

9. Axis 9 — Pass. No disposable-worktree prescription or parked/queued candidate content is introduced.

10. Axis 10 — Pass. The diff is confined to the canonical skill file’s two bullets and their provenance/evidence text.

FIX 1
