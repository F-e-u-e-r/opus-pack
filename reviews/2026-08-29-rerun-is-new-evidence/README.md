# Rerun-is-new-evidence — design + landing record (candidate ⑭)

**Landed rule:** `ground-truth-gates`, immediately after the producer-side preservation sentence, as its consumer-side dual — *preserve so it can be re-read; when auditing historical evidence, re-read that evidence identity. A rerun is a new evidence row, not a substitute for the old one.*

## Trail

1. **Orientation (read-only, falsification-first).** Nine adjacent passages (GTG preservation, persisted rows, regenerate-and-diff, verify-by-reconstruction's state-not-history clause; D&R current-artifact-overrides-history, survey-reverify, completion-audit; op-rigor check-name and destination re-read) each individually pass a constructed counterexample: an auditor never opens a cited 36/36 ledger, re-runs the recipe, gets a fresh 36/36, and declares the historical report verified — while a 35-row ledger, a crashed original run, a different config, a mistranscribed result, or a vanished artifact all survive that fresh PASS. **False verification with zero rule violations.**
2. **Adjudication: PARTIAL-GAP.** Identity infrastructure existed (producer-side preservation, per-run persisted rows, the state≠history admission); missing was ONE consumer-side evidence-identity rule. Canonical home: ground-truth-gates single carrier; no delegation-and-review mirror (its completion audit already cites GTG rules by name); op-rigor's check-name rule composes untouched. Locked semantics: evidence identity is content-verified, never pathname-bound — a verified content-identical archival copy (exact blob/hash- or run-id-matched relocation) IS the cited record; an unrecoverable record leaves the historical claim UNVERIFIED; fresh runs remain fully legitimate for current-state health, reproducibility, corroboration of an inspected record, regenerate-and-diff, and verify-by-reconstruction — the sole prohibition is silently substituting a rerun for the evidence identity under audit.
3. **Design review (r1, dual-blind, mutually blind, isolated dirs, identity from tool banners):** gpt-5.6-luna (max) — PROCEED · gpt-5.6-sol (max) — PROCEED. **2/2, zero blocking findings**; all ten mandatory axes answered line-anchored by both, including the dedicated misread check that "cannot recreate the missing history" cannot be read as forbidding durable-copy verification (both confirmed the archival carve-out is visible in the same rule and its verified-content-identical qualifier governs hash- and run-id-matched relocations alike). Both confirmed no runtime tooling, no mirror, no collateral rewrite. Verdicts in `verdicts/`.
4. **Landing.** The reviewed rule and Provenance blocks landed **byte-verbatim** (`FINAL-WORDING-RECONSTRUCTION.md`); declared landing adaptations: NONE.

## Status

The rule carries **exactly one** `unprobed` marker; provenance class is counterexample-derived / design-reviewed. **Behavioral transmission/effectiveness remains unprobed**; the future behavioral probe (bare vs ruled auditor handed a cited-ledger claim and a runnable recipe, scored on whether the ledger is opened before the claim is credited) joins the standing #115 queue. Static discriminating power only — see `STATIC-CONTROLS.md` (8/8 + 2 archival variants).
