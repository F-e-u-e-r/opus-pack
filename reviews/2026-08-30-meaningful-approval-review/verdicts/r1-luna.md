The core limb is well-bounded: it extends the existing human-decision principle, applies even when `[y/N]` remains, preserves the A1–A14 distinctions, and stays L2/supporting-only. The main defects are wording-level:

- “in order to obtain approval” introduces candidate-intent inference, conflicting with the observable-effect rule. Describe material impairment of risk review instead.
- “The tell is persistence…” is unscoped and could make a weaker model require persistence for every hit, missing one-shot steering, minimization, or batching. Qualify it as applying to repeated requests.
- The skill-vetting entry is not fully bare: its first sentence restates the criterion. Remove that sentence and retain only the routing pointer plus orthogonality note.

FIX intent-based risk clause, unscoped “The tell is persistence” sentence, non-bare skill-vetting pointer
