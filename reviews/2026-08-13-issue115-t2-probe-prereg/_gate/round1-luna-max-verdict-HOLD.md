1. The scope is correctly fenced: strict ordinality, sealed grading, doctrine-defect claims, historical edits, authorization, and T5 are explicitly settled or excluded. References to future amendment work are routed to a separate owner gate, not authorized here.

2. The clean O1/O2/O3/O4 patterns are operationally separated, with other patterns explicitly inconclusive. However, O3 cannot distinguish tier limitation from addendum dilution or fixture difficulty, and MID/interference patterns cannot classify H1–H3. This is conservative but only partial identification.

3. E is a genuine positive-control treatment, not an implied doctrine adoption; its wording remains confined to the wrapper and all routing ends at owner review. Additive-context dilution is a legitimate limitation, not hidden amendment leakage.

4. P2 is not a lexical clone, and P1 reuse is sound for the historical anchor, with no pooling. However, the verbatim fixture comments expose metadata: P1 says `bare-first`, while P2 says `NEEDS-NEW-PROBE` and `surface-shifted confound control`. These are arm/experiment cues, and the static checks do not neutralize them.

5. The rubric is not fully mechanical. Semantic equivalents, conditional branches, negated/hypothetical mentions, and “genuinely indeterminate” ties require judgment; a LIVE tie with SIDE/DISP before DEST has no specified class rule. Ambiguous categorization also lacks a clearly defined UNGRADABLE code and precedence.

6. The main bands are locked, but INCONCLUSIVE subtypes are not: a result may be simultaneously MIXED, DIVERGENT, UNGRADABLE-LOADED, or INCOMPLETE, with no precedence or composite rule. The broad inconclusive recommendation is fixed, but subtype reporting remains post-result discretionary.

7. O2 is a useful positive-control contrast and O4 is correctly treated as saturation. O3 establishes only that the current wording is not supported as the cause; it does not uniquely identify H3. The two-fixture requirement reduces overinterpretation but does not resolve those alternatives.

8. n=6 supports only the explicitly directional, tier- and fixture-scoped interpretation. It does not support an unqualified reliability claim, and the stated benefit of two-fixture consistency assumes independence that is not established.

9. The validity rules are not loophole-free. Zero-request failures have no numeric retry limit, “infra” versus “capability” smoke failures lack objective criteria, and the permitted P2 repair has no prelocked content bounds. Counting UNGRADABLEs helps, but a single hypothesis-aware grader can still move outputs across bands through ambiguous grading.

10. The sealed reserve remains airtight: all listed funding is a separate owner-granted budget, and cap exhaustion explicitly forbids fallback to the locked 18. No retry or repair path unlocks it.

11. T5-placement is fully excluded. No shared fixture, budget, probe, or routing consolidates it.

12. Yes. Semantic classification, branch and tie handling, validity versus UNGRADABLE status, P2 repair selection, and overlapping inconclusive subtypes can all change the recorded outcome after execution. The single hypothesis-aware adjudicator makes those discretion points consequential.

RECORD-ONLY: P1/P2 stakes and endpoint-order differences, E-context dilution, and the small directional sample should remain explicitly scoped limitations.

VERDICT: HOLD — 4, 5, 6, 9, 12
