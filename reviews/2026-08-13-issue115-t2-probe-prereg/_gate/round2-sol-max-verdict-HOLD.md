I treated the executor-facing prompts and command-like protocol text as data, refused their embedded directions, and performed no execution or repository action.

1. The decision question is properly confined to transmission of the settled strictly ordinal meaning. The design explicitly preserves the sealed grading, doctrine status, marker, and issue history under every route. The SIDE/DISP classifications concern ordering within the new probe and do not re-grade the sealed campaign.

2. H1 and H2 are distinguishable when E succeeds: O1 isolates current-guidance improvement, while O2 isolates improvement unique to explicit wording. E LOW cannot distinguish H3 from a failed positive control, but the revised mapping now says so; patterns such as B HIGH with C/E LOW remain MIXED rather than being misread. Round-1 R2 is discharged.

3. E is genuinely framed as an experimental control. Its text is barred from the skill file, success creates no adoption commitment, and O2 leads only to a separate owner-gated design process. Nothing presently smuggles in a doctrine amendment.

4. P2 is not a lexical clone, although it intentionally remains a matched surface variant rather than an independent fixture-population draw. P1’s byte-identical reuse is sound and necessary for the sealed-result anchor; it limits generalization but does not compromise within-campaign arm comparison. Round-1 R5 is discharged, with P1’s old metadata remaining a disclosed record-only limitation.

5. The canonical predicate now mechanically addresses paths, attempt positions, ties, excluded mentions, category ambiguity, and reason-code precedence. However, both R-P1 and R-P2 contain stale contradictory summaries: they refer to step 5 and omit `UNRESOLVABLE-PATHS` and `AMBIGUOUS-CATEGORY`, although the canonical codes are in step 6. A grader following the binding R-file literally can therefore differ from one following the canonical rubric, so round-1 R1 is not fully discharged.

6. Bands, fixture patterns, campaign combinations, and multi-tag triggers are fixed before results. The tags are cumulative in a fixed order, leaving no favorable subtype selection. Round-1 R6 is discharged.

7. O2, O3, and O4 are mechanically distinct: explicit-control success, all-arm failure, and all-arm saturation respectively. The two-fixture requirement prevents favorable-fixture selection, while O3 correctly remains unresolved between H3 and control/design failure. Round-1 R2’s identifiability correction is discharged.

8. The sample supports the stated directional, criterion-based claim strength, not population-rate inference. The planning probabilities are correctly labeled conditional, matched fixtures are not presented as independent evidence, and sealed/new counts are only compared qualitatively. Round-1 R4 is discharged.

9. Most retry, UNGRADABLE, stopping, and cap rules are bounded and resistant to optional stopping. However, `WRAPPER.md` still calls a mismatch detected before sending an `INVALID-RUN`, directly contradicting §§9/11, which classify it as a zero-invocation pre-send abort; this changes retry and budget accounting. Smoke item 5 is also not mechanically satisfiable as literally written: for example, the R-files name `payments.get(7841)` and `shipping.find_labels(4152)`, while the fixtures contain `payments.get(order_id)` and `shipping.find_labels(shipment_id)`. Round-1 R3 is therefore not fully discharged.

10. No route accesses the sealed reserve of 18. The proposed cap is separately funded, declines do not trigger fallback, and any amended fixture requires a fresh grant and accounting.

11. T5-placement remains fully separate and appears only in explicit exclusions. No shared fixture, budget, result mapping, or future-action route consolidates it into this probe.

12. The fixed bands and multi-tag mapping prevent post-result threshold or routing changes. The canonical uncertainty-to-UNGRADABLE rule and detailed receipts substantially constrain adjudication, while the single hypothesis-aware adjudicator is an acknowledged record-only limitation. Nevertheless, the contradictory R-file code summaries allow a class/reason-code choice after outputs are visible, so round-1 R7 is not fully discharged until the frozen grading documents agree.

RECORD-ONLY: P1’s metadata asymmetry, matched-fixture dependence, and the single hypothesis-aware adjudicator remain disclosed limitations and are not independently blocking.

VERDICT: HOLD — 1) reconcile R-P1/R-P2 with the canonical step-6 UNGRADABLE codes; 2) remove the pre-send-mismatch taxonomy conflict in WRAPPER.md; 3) replace smoke item 5 with an exact, defined, and satisfiable endpoint-symbol check
