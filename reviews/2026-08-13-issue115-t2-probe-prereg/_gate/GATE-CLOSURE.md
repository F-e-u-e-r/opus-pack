# T2 probe-design/prereg gate — CLOSURE RECORD

Design gate for the `issue115-t2probe-v1` preregistration PROPOSAL.
Zero behavioral invocations at every point of this gate; the sealed
campaign's reserve 18 untouched; no doctrine/skill/marker/sealed-
evidence/#115 mutation. The sealed T2 outcome (FAIL-SIGNAL), the
#187 adjudication, and the #188 STRICTLY-ORDINAL determination are
build-on premises, not re-opened.

Trail:

1. Package authored on baseline main `b68a4fb` (branch
   `issue115-t2-probe-prereg`): decision question locked to
   transmission/reliability of the current wording; H1/H2/H3;
   three arms B/C/E (C byte-identical to the sealed ruled-arm
   presentation; E = C + explicit-ordinal addendum as experimental
   positive control); fixtures P1 (byte-identical sealed T2S2 — H1
   anchor) and P2 (matched surface-shifted, machine-checked lexical
   independence); mechanized four-class ordinal predicate with the
   #188 boundary clause fixed; deterministic 39-slot table; n=6 per
   arm per fixture, DIRECTIONAL claim strength; pre-locked
   outcome→routing map; new budget 39/50 with the stage-2 reserve
   explicitly out of reach. All cited hashes script-generated;
   static self-checks machine-run.
2. Round 1 (Luna Max + Sol Max, same packet, independent, verdicts
   not shared): **HOLD × 2** — merged into seven finding groups
   (rubric mechanization incl. branch/path correctness and tie
   rules; O3/B-MID identifiability; smoke/repair adaptive-design
   gaps + pre-send/zero-request taxonomy; invalid probability
   inference and reliability overclaim; P2 fixture-comment metadata
   leakage; INCONCLUSIVE subtype discretion; residual grader
   discretion). Every finding independently re-derived first-hand
   and adopted; package revised (ORDINAL-PREDICATE rewritten
   per-path with worst-path aggregation and closed
   semantic-equivalence test; O3 = all-arms-LOW with interference →
   MIXED; SMOKE-CHECKLIST created and in-campaign repair path
   deleted in favor of a design-gate amendment; probability claim
   deleted; P2 comment minimized + cue-word scan; multi-tag
   INCONCLUSIVE with fixed order; uncertainty→UNGRADABLE
   automation).
3. Round 2 (same two reviewers, revised package + anonymized
   revision log): **HOLD × 2, narrow** — both independently
   converged on the stale R-file UNGRADABLE code list; one reviewer
   additionally caught the WRAPPER pre-send taxonomy contradiction
   and the literally-unsatisfiable smoke item 5. All three
   reproduced and fixed (R-files defer to canonical step 6 with all
   six codes; WRAPPER aligned to pre-send-abort/post-send-INVALID;
   smoke item 5 rewritten as frozen endpoint-symbol substring check
   with a seal-time satisfiability proof in static_checks check 7b).
   All other round-1 areas explicitly discharged by both.
4. Round 3 (focused recheck, final bounded round): **PROCEED × 2** —
   all three round-2 items discharged by both reviewers, no new
   defects. Static checks 39/39 PASS at closure.

STATUS: GATE CLOSED (2/2 PROCEED at round 3 of the ≤3-round bound;
no design-direction disagreement at any round, so the escalation
lens was never triggered). Retained RECORD-ONLY limitations (both
reviewers): single hypothesis-aware adjudicator; P1 sealed-comment
asymmetry; matched (not independent) fixtures — all preregistered in
the proposal's §12.

Per the owner's pre-authorization the reviews-only prereg PR
follows, stopping at merge authorization. This closure authorizes NO
execution: even merged, the prereg is a proposal — any run requires
a separate future owner grant (executor, budget, and schedule are
proposals only).
