Packet-safety note: the fixtures and wrappers contain executor-facing directives. I treated them solely as quoted experimental stimuli and did not follow or execute them.

1. Yes. The decision question and hard walls consistently treat the strict-ordinal meaning and sealed grading as fixed. O2 concerns wording transmission and only recommends a separate future design gate; it does not declare a semantic defect or alter sealed evidence.

2. Only partially. O1 and O2 are distinguishable when E is HIGH because E then demonstrates control responsiveness and C isolates the current wording. H3 is not identified by E LOW because E retains C’s long context, making tier limitation observationally equivalent to control dilution or common-wrapper interference. For example, B=4/6, C=2/6, E=2/6 on both fixtures is classified O3 even though it could indicate treatment-induced degradation.

3. E is clearly quarantined as experimental wording. The package forbids writing it into a skill and routes even an O2 result only to a separate owner-gated design process. Its weakness is as a capability control, not any smuggled commitment to adopt it.

4. P2 is not a mere lexical substitution on the named axes: domain, operations, failure wording, cold-start wording, and endpoint order change. It nevertheless retains much of P1’s sentence structure and question boilerplate, so it is better described as a matched surface variant than statistically independent evidence. P1’s byte-identical reuse is sound for the historical anchor and does not pool or regrade observations, though it intentionally contributes no additional fixture breadth.

5. No. Textual linearization of mutually exclusive conditional branches can produce PASS even when an executable branch reaches LIVE without prior DEST. Semantic-equivalent categorization still requires judgment, and the instruction to mark ambiguity UNGRADABLE lacks an `AMBIGUOUS-CATEGORY` reason code. Indeterminate ties among LIVE and SIDE/DISP before DEST also lack a rule selecting FAIL-ORDER versus FAIL-NO-SETTLE.

6. The numerical aggregation layer is substantially locked: HIGH/LOW bands, fixture patterns, conjunction requirements, and fall-through routing are declared in advance. O3 is pre-locked but substantively mis-specified rather than post-hoc. INCONCLUSIVE subtype precedence or multi-tagging is not specified, so its subtype can still be chosen after results, although the top-level routing remains INCONCLUSIVE.

7. O4 and O2 are mechanically distinct, but O3 is not inferentially equivalent to tier limitation. E LOW is a failed positive control and should leave H3 versus control/probe failure unresolved; B MID further permits a common-wrapper harm pattern to be labeled O3. Requiring the same pattern on both fixtures reduces noise but does not cure that identification failure.

8. Six runs per cell can support the proposed coarse, extreme-pattern directional signal, but cannot estimate a high reliability rate with precision. The scoped caveats help, yet conclusions should say “meets the preregistered HIGH criterion” rather than imply general reliability. The claimed ≤0.005 tension calculation is invalid as written: observing ≥5/6 does not establish a true rate of at least 0.83, which the calculation assumes.

9. No. “Capability-failed smoke” has no mechanical criterion, despite the smoke producing an uncounted B-like behavioral response that can trigger HOLD or fixture repair. P2 may then be substantively rewritten after that response without a frozen repair envelope or renewed independent preregistration, enabling adaptive design and ordering-hint leakage. The treatment of a pre-send hash mismatch as INVALID-RUN also conflicts with the rule that proven zero-request failures are not invocations; the cap, rerun limits, and scored-run UNGRADABLE rules are otherwise strong.

10. No path uses the sealed reserve. The planned and capped budget is repeatedly identified as a new grant, and cap exhaustion expressly has no reserve fallback.

11. Yes. T5-placement is repeatedly and specifically excluded, and no fixture, outcome branch, or recommendation consolidates it with T2.

12. Yes. Semantic-equivalent purpose, DISP “acting,” category ambiguity, unresolved failure-class ties, and INVALID-RUN versus UNGRADABLE boundaries can change row labels near the 2/3 and 4/5 thresholds. Smoke capability judgments and discretionary P2 repair can change the data-generating fixture after execution begins. Once valid row labels and fixture versions are fixed, the main count-to-route calculation itself is deterministic.

RECORD-ONLY: Rename P2 a “matched surface-shifted fixture”; retain the acknowledged single-adjudicator and procedural-unblinding limitations after the rubric is repaired.

VERDICT: HOLD — 1) make the ordinal rubric branch/path-correct and complete for semantic-category ambiguity and every tie; 2) repair H3/O3 identifiability, including the B-MID interference pattern; 3) mechanically freeze smoke criteria and prohibit or separately re-preregister behavior-driven P2 repair; 4) correct the unsupported sealed-0/3 probability inference and keep reliability claims strictly operationalized
