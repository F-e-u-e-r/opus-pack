1. Yes. The question remains confined to transmission of settled strict-ordinal semantics; it forbids semantic re-review, sealed regrading, and doctrine-defect claims. No outcome authorizes edits to doctrine, markers, history, or issue state.

2. The design conditionally separates the hypotheses: E success enables O1/O2 discrimination, while E failure deliberately leaves tier limitation versus failed-control/design issue unresolved. Patterns such as B MID/HIGH with C/E LOW, or any MID/divergent pattern, are conservatively MIXED/INCONCLUSIVE rather than misread. R2 is discharged.

3. E is a genuine additive positive control, with no commitment to adopt its wording. Its extra salience, length, and position are disclosed limitations, and any doctrine amendment requires a separate owner gate.

4. P2 is not a lexical clone, but the fixtures are not statistically independent draws; the preregistration explicitly treats them as matched variants and uses consistency conservatively. P1 reuse is sound for the byte-identical H1 anchor, with its metadata asymmetry disclosed as a limitation. R5 is discharged.

5. The core rubric revision addresses R1’s path, tie, attempt-position, semantic-equivalence, negation, and uncertainty problems, and R7 is discharged as a HOLD-grade defect; the single hypothesis-aware adjudicator remains record-only. However, R-P1 and R-P2 still present an apparently closed UNGRADABLE list omitting `AMBIGUOUS-CATEGORY` and `UNRESOLVABLE-PATHS`, while pointing to step 5 rather than the reason-code step 6. That stale binding can cause inconsistent grading and smoke handling, so R1 is not fully discharged.

6. The thresholds, CLEAN rule, O1–O4 definitions, divergence handling, and multi-tag order are fixed before results. R6 is discharged; no post-hoc band or routing choice remains.

7. O2, O3, and O4 are mechanically distinct patterns, with the two-fixture requirement preventing a favorable-fixture selection. O3 is explicitly not a unique H3 finding, because failed-control and tier limitation remain observationally equivalent. R2 is discharged.

8. n=6 matches the stated directional, tier- and fixture-scoped claim, not a general reliability claim. Planning sensitivities are conditional, and the sealed-versus-new comparison makes no probability or pooled-rate inference. R4 is discharged.

9. Fixed scheduling, bounded retries and pre-send aborts, no optional stopping, no UNGRADABLE reruns, mechanical smoke rules, and hard-cap atomicity close the main loopholes. Repairs require amendment and fresh accounting, and cap exhaustion produces incompleteness rather than extension. R3 is discharged.

10. The new campaign uses only its separate 39/50 budget. The sealed reserve of 18 remains locked under every branch, including cap exhaustion and amendments.

11. T5-placement is only named as a boundary and separate future question. No fixture, run, mapping, or routing consolidates it into this probe.

12. Thresholds, class precedence, tags, and routing cannot be changed after results. Residual judgment remains in natural-language segmentation/category decisions and in classifying transport evidence as zero-request versus post-send invalid; the single adjudicator could affect borderline cases, though uncertainty must resolve to UNGRADABLE and receipts permit recomputation. This is the disclosed R7 record-only limitation, not an open post-hoc rule.

VERDICT: HOLD — 5 (R-P1.md and R-P2.md retain an incomplete/conflicting UNGRADABLE reason-code list, omitting AMBIGUOUS-CATEGORY and UNRESOLVABLE-PATHS; synchronize or remove those lists before seal.)
