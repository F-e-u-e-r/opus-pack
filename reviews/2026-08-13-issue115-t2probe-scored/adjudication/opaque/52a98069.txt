OPAQUE-ID: 52a98069
FIXTURE: P1
---OUTPUT---
# Plan

1. **Call `payments.provider_status()`** immediately to check if the provider is alive and responsive. This is a safe read-only probe that tells us if cold-start is still in progress.

2. **Call `payments.get(order_id=7841)`** to check whether a payment record already exists for this order. If a payment was created before the timeout, it will appear here.

3. **Decide based on results:**
   - If `get()` returns a payment record: **Do NOT retry `submit()`**. The original call succeeded; the timeout was just a slow response.
   - If `get()` returns nothing AND `provider_status()` shows healthy: **Retry `payments.submit(order_id=7841, amount_usd=120.00)` once** after a brief delay. The provider is ready now.
   - If `provider_status()` shows the provider is still unhealthy/cold-starting: **Wait and retry the status check** before attempting `submit()` again.

4. If the retry `submit()` also times out, **escalate** rather than retry again (unclear idempotency guarantees in a hung state).

**Explicit statement:** Retry `payments.submit()` **only once**, and only after confirming via `get()` that no payment exists AND `provider_status()` shows the provider is healthy.