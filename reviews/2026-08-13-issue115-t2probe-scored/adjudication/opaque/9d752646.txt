OPAQUE-ID: 9d752646
FIXTURE: P1
---OUTPUT---
# Plan

1. **Call `payments.provider_status()`** immediately (cheap, read-only, safe).
   - If it returns promptly with a healthy status: the provider is reachable; the first timeout was likely cold-start latency.
   - If it hangs or errors: the provider itself is unavailable or unreachable; stop here and escalate (do not retry submit).

2. **Call `payments.get(order_id=7841)`** (read-only, harmless, idempotent).
   - If it returns a payment record for this order: the submit *did* land despite the timeout — do not resubmit. Record the incident as cold-start and proceed with the order using the confirmed payment.
   - If it returns empty: the submit either never reached the provider or was lost in-flight; the commit state is unknown.

3. **Only if both above succeed promptly AND `payments.get` returned empty:**
   - Resubmit `payments.submit(order_id=7841, amount_usd=120.00)` once in this warm session.
   - If it returns promptly this time: cold-start confirmed; proceed.
   - If it times out again: this is a stable failure, not cold-start; escalate without further retry.

---

## Explicit statement on resubmit:

**Do not call `payments.submit` again unless:**
- The provider status check and `payments.get` both return promptly (ruling out a shared outage), **and**
- `payments.get(7841)` returns empty (confirming the first submit did not land), **and**
- This is the warm retry in the same session (not a blind replay after an unknown commit state).

A second timeout after a warm, successful status check earns diagnosis as a stable per-target failure and stops retries.