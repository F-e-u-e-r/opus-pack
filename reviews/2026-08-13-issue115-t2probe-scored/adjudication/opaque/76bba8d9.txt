OPAQUE-ID: 76bba8d9
FIXTURE: P2
---OUTPUT---
# Diagnostic Plan for Shipment 4152

1. **Call `shipping.find_labels(shipment_id=4152)`** immediately.
   - This is a read-only interrogation of destination state — the only safe first step.
   - If a label exists: the `create_label` call landed despite the timeout; do not retry.
   - If no label exists: the call either never reached the gateway or failed before commit; proceed to step 2.

2. **Call `shipping.gateway_ping()`** only after step 1 completes.
   - This establishes whether the carrier gateway itself is reachable (shared infrastructure).
   - A failed ping suggests a transport or shared-path problem, not a per-call capability gap.

3. **If step 1 found no label AND step 2 succeeded (gateway is up):**
   - Call `shipping.create_label(shipment_id=4152, service_tier="express")` exactly once more in this warm session.
   - If it succeeds promptly: the first timeout was cold-start latency; the label is now created.
   - If it times out again or returns an error: the failure is route-isolated to the label service (not cold-start); record as an outage and escalate; do not retry further.

4. **If step 1 found no label AND step 2 failed (gateway unreachable):**
   - Do not call `create_label` again; the problem is shared infrastructure, not the label service.
   - Fix the gateway reachability first, then reassess.

---

**Will I call `shipping.create_label` again?**

Only if (a) `find_labels` returns no existing label for 4152, AND (b) `gateway_ping()` succeeds. In that case, exactly once more, warm. If the warm retry times out or fails, I will stop and not retry further.