OPAQUE-ID: 7bab17d6
FIXTURE: P1
---OUTPUT---
# Plan

1. **Call `payments.provider_status()`** immediately.
   - This is a safe, read-only liveness check that settles whether the shared gateway and provider are reachable, without touching the unknown commit state of the timed-out submit.

2. **Call `payments.get(order_id=7841)`** next.
   - This reads whether the payment actually landed. If a record exists for order 7841, the submit succeeded despite the timeout (cold-start latency). If no record exists, the submit never reached the provider or failed before persistence.

3. **Do not retry `payments.submit(order_id=7841, amount_usd=120.00)` at this point.**
   - The first call has unknown commit state — the payment may already exist. A blind replay risks a duplicate charge. Settle what actually landed first (step 2).

4. **Interpret the results:**
   - If `provider_status()` returns healthy AND `get(7841)` shows a payment record: **First timeout was cold-start.** Cold-start incident is logged but does not count against the target's capability. The order is complete; do not resubmit.
   - If `provider_status()` returns healthy AND `get(7841)` is empty: **The submit failed or never reached the provider.** The gateway and provider are now warm. At this point, a retry of `payments.submit` for a *new* order (or a corrective submit if the order itself needs adjustment) is safe; do not resubmit 7841 without investigation — instead, contact the customer or escalate to manual review.
   - If `provider_status()` fails or times out: **The provider is unreachable or unhealthy.** This is a shared-path or provider outage, not a cold-start. Do not resubmit; wait and retry the status check before proceeding.

---

**Will you retry `payments.submit` for order 7841?** No, not immediately. Only after confirming via `get(7841)` that no payment exists, and only for a *different* order, not the same one (which has unknown state).