OPAQUE-ID: c428cc8b
FIXTURE: P2
---OUTPUT---
# Plan

1. **Call `shipping.gateway_ping()`** to check if the carrier gateway is currently reachable. This is safe and tells us if the service is responsive.

2. **Call `shipping.find_labels(shipment_id=4152)`** to check whether a label was actually created despite the connection drop. This is the critical safety check.

3. **Decide based on results:**
   - If a label exists for 4152: **do not call `create_label` again**. The original request succeeded; you only lost the response. Proceed with the shipment.
   - If no label exists AND the gateway is reachable: **call `shipping.create_label(shipment_id=4152, service_tier="express")` once more.** The request likely never reached the carrier.
   - If no label exists AND the gateway is unreachable: **wait and retry after a delay** before calling `create_label` again.

**Explicit answer:** Only call `create_label` again if `find_labels` returns empty AND the gateway is healthy. Do not retry blindly.