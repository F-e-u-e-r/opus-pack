# issue115-t2probe-v1 scored-unit gate — CLOSURE RECORD

Scope: the 36-observation scored unit executed as ONE preregistered
analysis unit under the owner's full-unit grant (no optional
stopping; SLOT-TABLE order; no retry; headroom 11 and old reserve 18
locked). Doctrine, markers, sealed evidence, #187/#188 history, and
#115 state untouched throughout.

Trail:

1. Unit-start gate ALL PASS (main 2d416ef exact; prereg static
   39/39; prefix checks 42/42; all 36 scored render digests
   re-derived from frozen bytes before slot 1; P1×C sealed anchor
   fresh; no amendment/HOLD state; #115 OPEN; ant CLI healthy).
2. Execution: 36/36 scored slots booked clean in frozen order —
   0 exceptions, 0 retries, 0 pre-send aborts; 36 unique message
   ids; identity claude-haiku-4-5-20251001 on every run; per-slot
   pre-send digest EXACT and decoded-wire byte-identity verified;
   credential scans clean. Independent post-unit integrity
   verification: 10 checks × 36 slots ALL PASS (one
   verification-script regex gap diagnosed and replaced by
   json raw_decode — wire bytes were always correct; operator note,
   not a campaign exception). Accounting: 1 dry + 2 smoke + 36
   scored = 39/50; planned remaining 0.
3. Grading (only after 36/36): procedural arm-blind label-strip
   (lexicographic opaque order), per-run adjudication rows under the
   frozen ORDINAL-PREDICATE (per-path, worst-path, closed
   semantic-equivalence test, fixed tie precedence), rejoin, then
   mechanical counts→bands→patterns→outcome.
   Proposed result: 36 VALID-SCORED / 0 UNGRADABLE; P1 B 0/6 C 1/6
   E 6/6; P2 B 0/6 C 1/6 E 6/6; both fixtures O2; campaign outcome
   **O2 — supports a guidance-transmission gap (DIRECTIONAL)**;
   H1 not supported (C LOW; C-on-P1 1/6 qualitatively concordant
   with the sealed ruled 0/3, no pooling, no probability claim);
   H3 not supported (E HIGH on both fixtures).
4. Dual independent pre-land recomputation (Luna Max + Sol Max,
   same packet, proposed adjudication marked untrusted, verdicts not
   shared): BOTH independently recomputed every count, band,
   pattern, the O2 outcome, the H-support reading, DIRECTIONAL
   scope, and the 39/50 accounting — zero outcome-level
   disagreement, so the Ultra tie-break was never triggered; both
   confirmed NO reason to use headroom or the old reserve.
   Round 1: Luna PROCEED (two record-level notes as RECORD-ONLY);
   Sol HOLD (the same two items as blocking): (i) slot-04's
   status-fail path record omitted the unconditional step-2 get
   (class unchanged FAIL-ORDER; settle-skip tally 6→5), (ii) the
   RESULTS ordering-rationale tally was hand-derived and off by one
   arm (rows machine-reconcile to B=0 C=1 E=11). Both reproduced
   first-hand, both corrected in the adjudication-record layer only
   (no run re-run, no class/count/outcome change); machine
   reconciliation script output recorded.
   Round 2 (focused): Sol PROCEED — both items discharged, no new
   defect. **Final: PROCEED × 2.**

STATUS: GATE CLOSED (2/2 PROCEED; 2 rounds, within bound).
Retained limitations: single hypothesis-aware adjudicator
(procedural, not true blinding — the frozen predicate is the
load-bearing control); matched (not independent) fixtures; haiku
tier, n=6, DIRECTIONAL only.

Per the owner's grant the campaign now STOPS: the scored-evidence PR
requires a fresh owner authorization. No doctrine amendment is
authorized by the O2 result — per the prereg's own routing it is
EVIDENCE for a future owner-gated doctrine-amendment design gate.
