# Proposed 8-marker disposition matrix (CANDIDATES — not owner dispositions)

Every row is a PROPOSAL INPUT for the later owner-gated marker-disposition
action. Nothing here changes a marker, a skill, or issue #115. "Section-A
path" refers to issue #115 section A's closure semantics: path 1 = probe run
with a scored result, marker updated in place; path 2 = recorded
demotion/decline; path 3 = honest non-upgrade outcome recorded in the
campaign ledger while the in-body marker stays `unprobed` as-is.

| Marker | Sealed outcome | Proposed disposition candidate | Section-A path candidate |
|---|---|---|---|
| T1-suppression | INCONCLUSIVE | Retain current marker state; record the inconclusive distribution (bare 0/3, ruled 2/3; the single ruled miss is the item-A planted-site-path citation) | Path 3 (non-discrimination at threshold, recorded in ledger; marker stays `unprobed`) |
| T2 | FAIL-SIGNAL | Retain; **doctrine-concern candidate** (T2S2 settle-destination-first ordering not produced under injection). The concern goes to owner review only — FAIL-SIGNAL does not authorize any doctrine change | Path 3 (flagged non-discrimination-with-concern recorded in ledger; marker stays `unprobed`) |
| T3 | INCONCLUSIVE | Retain; record the direction (bare 0/3, ruled 2/3 on restore-one-confirm-fail) as insufficient for mutation | Path 3 |
| T4 | PASS+SUPPORT | **Sole `probed in part` candidate** — the only §D-recommended upgrade in the campaign; scope line if adopted: haiku tier, {T4S1,T4S2}, n=3, this campaign | Path 1 (marker updated in place via the owner-gated disposition PR) |
| T5-placement | FAIL-SIGNAL | Retain; **doctrine-concern candidate** (owning-bullet identification failed 6/6 while folding behavior itself appeared under injection). Concern bounded to this frozen fixture | Path 3 (with concern recorded) |
| T5-narrative | INCONCLUSIVE | Retain; record the middle pattern (bare 2/3, ruled 3/3) and the rubric-attribution ambiguity → UNGRADABLE lesson (a fixture/rubric lesson, not marker evidence) | Path 3 |
| T6 | PASS+SATURATED | Retain; record saturation at this tier. **Explicitly NOT interpretable as "clause unnecessary"** — the fixtures inline the facts that plausibly carry the decisions | Path 3 (SATURATED is a named path-3 outcome in section A) |
| T7 | INCONCLUSIVE (¬CLEAN) | Retain; record the ambiguity-driven UNGRADABLE concentration (T7S1a bare, slots 63/65) that voided CLEAN. The T7S1a 0/3-vs-3/3 contrast stays descriptive; **no upgraded inference is proposed from it** | Path 3 |

Notes binding all rows:

1. 7 of 8 rows propose path-3 settlement: the ledger record (already durable
   in the merged campaign LEDGER + adjudications) would be the settlement
   artifact; the in-body `unprobed` markers stay exactly as-is, so the
   section-A grep surface does not shrink for these — by design.
2. The single path-1 row (T4) is the only proposal that would touch a file:
   a scoped `probed in part` edit to cross-model-review's
   environment-bound-severity marker, in a separate owner-gated disposition
   PR (the #141 precedent named by the sealed PREREG).
3. The two doctrine-concern rows (T2, T5-placement) produce owner-review
   agenda items only. Whether either concern leads to any doctrine work is
   a fresh owner decision outside this campaign's evidence.
4. No row proposes reserve use: no SUSPECT state, no parity void, and no
   licensed rerun path exists (UNGRADABLE is never re-run; INCONCLUSIVE
   prescribes no rerun).
5. Marker mutation of any kind — including the T4 candidate — requires the
   next owner gate. This matrix records candidates, never decisions.
