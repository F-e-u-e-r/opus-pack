# Campaign-synthesis gate — CLOSURE RECORD

Trail:
1. Read-only synthesis phase (owner grant): 0 behavioral invocations; no
   marker/doctrine/skill/issue-state change; reserve untouched. Baseline
   locked at main ac38c3acc66310268a4db7095fc00a16abc1ff37.
2. The 8-marker matrix, campaign accounting, and every load-bearing number
   were machine-rebuilt from durable merged bytes (adjudication JSONs,
   receipts, LEDGER) — memory summaries were not treated as authority. Two
   drafting-time claims in the closure assessment were caught by the same
   mechanical verification and corrected before review: the section-A grep
   scale (true value 234 occurrences / 12 files at merged main) and the
   section-E state (evals/ is git-ignored and absent from the durable
   tree; the LOCAL run4.sh:26 already carries an ABORT-if-unset
   sandbox-root guard, so E's exit is "not verifiable from durable
   bytes", not "confirmed open").
3. Dual independent gate (Luna Max + Sol Max, same packet, verdicts not
   shared with each other): **PROCEED × 2**, all ten determinations
   affirmed by both — outcomes correctly transcribed; accounting exact
   (1/13/78, 75+3, 92/110, planned 0, reserve 18, discharged 0); no
   disposition exceeds evidence strength; T4 the sole `probed in part`
   candidate; T2/T5-placement limited to doctrine concerns; ambiguity
   lessons separated from marker evidence; T6 saturation not read as
   "clause unnecessary"; #115 closure recomputed against the issue's own
   sections A–E (not closeable; B–D unmet, E not verifiable from durable
   bytes, A a populated standing queue); no reserve reason exists; all
   mutations left to a future owner gate.
4. RECORD-ONLY dispositions: the shared seven-targets-vs-eight-markers
   clarification was added to SYNTHESIS.md §3 (wording only). Remaining
   notes recorded here without document changes: formal-taxonomy wording
   hygiene for non-sealed uses of outcome-class words; future tooling
   could report unique in-body markers separately from grep occurrences;
   "standing queue, currently nonempty" as alternative phrasing; T2's
   path-3 record may be phrased "FAIL-SIGNAL/no-upgrade with owner
   concern" since T2S1 itself discriminated; distinguish already-landed
   campaign evidence from future owner-gate settlement recording.

STATUS: SYNTHESIS GATE CLOSED (2/2 PROCEED, zero HOLDs, zero
outcome-changing disagreements — no Luna Ultra tie-break needed). Per the
owner's pre-authorization the reviews-only synthesis PR follows, stopping
at merge authorization. Actual marker mutation, doctrine action, reserve
use, and #115 closure remain owner-gated future actions.
