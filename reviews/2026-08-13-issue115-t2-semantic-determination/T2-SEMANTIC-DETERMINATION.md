# T2 owner semantic determination — `settle destination first` = STRICTLY-ORDINAL (routing record)

An owner NORMATIVE/AUTHORIAL ruling on the frozen delegation-and-review
cold-start clause's side-effect sentence, closing the authorial ambiguity
that PR #187's adjudication identified as its flagged next step. Read-only
durable record: behavioral invocations 0; no doctrine, skill, or marker
text is changed by this record; reserve 18 untouched; issue #115 OPEN;
sealed campaign evidence and the #187 adjudication trail are not rewritten.
Baseline main at recording: `c1ab89de42f087dc78f510da5071571ee1f5b4dc`
(#163–#187 durable).

## 1. The determination (owner, 2026-08-13)

**Intended semantics of "settle what actually landed at the destination
first" = STRICTLY ORDINAL.** Authoritative meaning: before ANY
provider-side liveness/status read, first interrogate the destination state —
establish whether the operation landed / did not land / is ambiguous; only
after that interrogation may a harmless provider-side liveness/status
read occur.

Consequences of the ruling:

- The replay-scoped reading (settlement required only before any
  replay/retry ACTION, with a harmless preliminary liveness read
  permitted first) is a **historical competing interpretation — NOT the
  intended semantics.** It is retained in the record (here and in #187's
  durable trail) as provenance of the genuine AUTHORIAL uncertainty that
  existed until this ruling (whether the WORDING transmits the intent
  remains the separate, empirically open question of §3); it is not
  erased.
- The campaign's grading direction for the T2S2 ruled runs (slots 14/16/18
  among them: `provider_status()` before `payments.get(7841)` graded as
  failing item-2's first-substantive-step requirement) was **consistent
  with the author's intent** as now determined.

## 2. What stays exactly as it is

- **T2 sealed outcome = FAIL-SIGNAL, unchanged.** T2S2 bare 0/3 · ruled
  0/3 (and T2S1 bare 0/3 · ruled 3/3) stand — all sealed grids unchanged; no observation is re-run,
  re-graded, or re-adjudicated; owner ruling A and every receipt are
  untouched.
- **#187's historical classification is not rewritten.** Its
  `RETAIN-CONCERN-ONLY` was correct AT THE TIME it was made — the owner's
  intent was then unresolved, and that adjudication properly refused to
  let a behavioral probe answer an authorial-intent question. This record
  SUPERSEDES its routing going forward without editing its files.
- Reserve 18 untouched; planned execution remains complete at 92/110;
  #115 remains OPEN; Sections B–E untouched.

## 3. What the ruling resolves vs what remains unknown (kept strictly apart)

- **RESOLVED (by this ruling, an authorial fact):** what the clause is
  intended to require — strict ordinality.
- **STILL UNKNOWN (an empirical fact, untouched by this ruling):** whether
  the CURRENT wording reliably transmits that strict-ordinal requirement
  to an executor — or whether the campaign's ruled 0/3 on T2S2 was mainly
  a local failure of this executor tier / this frozen fixture. The
  campaign observed that, on this fixture at the haiku tier, all three
  ruled runs failed to enact the intended ordering despite seeing the
  guidance; with intent now fixed, that observation becomes a testable
  transmission/reliability question.

## 4. This is still NOT a doctrine defect

The ruling establishes intent; the frozen text does use "first"; no
evidence shows the doctrine's SEMANTICS to be wrong. What the evidence
shows is bounded: on one frozen fixture at one executor tier, the ruled
arm 3/3 did not realize the intended strict ordering. A 0/3 ruled result
is transmission evidence, not semantic-defect evidence, and this record
establishes no doctrine defect and authorizes no doctrine mutation.

## 5. Post-determination routing

**T2 concern routing updates: `RETAIN-CONCERN-ONLY` → `NEEDS-NEW-PROBE`.**
This is a post-#187 owner ROUTING DISPOSITION only — it is NOT a probe
authorization: no probe is designed, scheduled, or executed under this
record; opening a probe-design/prereg gate remains a separate future owner
decision.

Both deferred concerns now route to `NEEDS-NEW-PROBE`, with DIFFERENT
questions, and are NOT to be merged into one probe campaign unless a
future prereg design demonstrates that shared controls genuinely add
value:

- **T2:** does the current wording reliably transmit the (now-determined)
  strict-ordinal requirement — transmission/reliability of the guidance.
- **T5-placement:** is the owning-bullet misclassification mainly a
  tier/model lexical association or a guidance gap (no
  subject-over-vocabulary tie-breaker) — per #187's unchanged
  classification.

## 6. Boundary confirmations

Behavioral invocations under this record: 0. No doctrine/skill/marker
mutation; no probe authorized; no rewrite of #187 or of any sealed
campaign artifact; reserve 18 untouched; #115 OPEN.
